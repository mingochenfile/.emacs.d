;; auto-generated
(defconst mu4e-mu-version "0.9.9.5"
  "Required mu binary version; mu4e's version must agree with this.")

(defconst mu4e-builddir "/home/commanders2005/.emacs.d/mu4e"
  "Top-level build directory.")

(provide 'mu4e-meta)
